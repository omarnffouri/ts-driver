# Local service configuration

This public snapshot intentionally omits Firebase configuration files and replaces embedded Google API keys with `REPLACE_WITH_GOOGLE_API_KEY`.

Before building, download configuration files for your own Firebase projects and place them in the appropriate Android flavor and iOS environment directories. Keep those files untracked.
